*.log
.DS_Store
.env
.env.backup
/.env.production
.phpactor.json
.phpunit.result.cache
/.fleet
/.idea
/.nova
/.phpunit.cache
/.vscode
/.zed
/auth.json
/node_modules
/public/build
/public/hot
/public/storage
/storage/*.key
/storage/pail
/vendor
_ide_helper.php
Homestead.json
Homestead.yaml
Thumbs.db
*.zip
/.claude

# Scribe internal extraction state — regenerated on every scribe:generate.
# (The rendered view at resources/views/scribe/ IS committed.)
/.scribe

# Local pre-migration backups created by dev workflow
/storage/backups

# Firebase service-account JSON (FCM push) — buyer downloads from
# Firebase Console; never commit. See .env.example for setup.
/storage/app/firebase-credentials.json

# Bruno CLI temp files (per-run state, never want in git)
/docs/bruno/**/bruno-lock.json
/docs/bruno/**/.bruno-cli
